#!/usr/bin/env bash

if [[ -z "$1" ]]
then
	args="."
else
	args="$@"
fi

# df -h "$args" | perl -pe 's/^.+? +([\d\.]+\w+ +){2}//g' | grep -iPo '^[\d\.]+[^ ]+'
df -h "$args" | column --table --output-separator $'\t' | cut -f4 | tail -1
