#!/usr/bin/env bash

# word diff
# at some point this stopped working in .gitconfig
# git diff --color-words="\w+|." --ignore-space-change "$@"
# change to this format for termux compatibility
git diff --color-words='\\\\w+|.' --ignore-space-change "$@"
