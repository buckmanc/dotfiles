# use -OJ to download files (--remote-name --remote-header-name)
# use --disable as the *first* argument to ignore this file

anyauth
create-dirs
compressed
# breaks curl spotify api on some platforms
# tr-encoding
insecure
location
# breaks pkg on termux
# remote-header-name 
parallel
progress-bar

# breaks curl spotify api
# header = "Accept-Language: en-US,en;q-0.8"
header = "Accept-Encoding: gzip, deflate, sdch"
header = "Upgrade-Insecure-Requests: 1"
referer = "https://www.google.com;auto"
user-agent = "Mozilla/5.0 Gecko"
connect-timeout = 30
# proxy http://proxy.local:1337
